@model IEnumerable<TuVanDuHoc_v2.Models.QUOCGIA>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TENQG)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TENQG)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MAQG }) |
            @Html.ActionLink("Details", "Details", new { id=item.MAQG }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MAQG })
        </td>
    </tr>
}

</table>
